; npm on Windows defaults to invoking package.json scripts via cmd.exe, which
; mis-parses this project's path (the folder name contains "&") and fails
; every "npm run ..." command with "'Found' is not recognized as an internal
; or external command". Forcing PowerShell as the script shell avoids that
; cmd.exe path-splitting bug.
script-shell=powershell.exe
